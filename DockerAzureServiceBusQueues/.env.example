# Copy this file to .env and customize before running docker-compose.
#   cp .env.example .env
#
# SQL Server requires a strong SA password (complexity rules apply).
# Do not commit .env with real passwords.

CONFIG_PATH=./config.json
MSSQL_SA_PASSWORD=Your_Strong_SA_Password_Here1
