# Copy to .env and customize before building the Content Hub components.
#   cp .env.example .env
#
# VITE_ values are embedded into the JS bundle at build time.
# Do not commit .env with environment-specific hostnames.

VITE_CMS_BASE_URL=https://your-sitecore-cm.example.com
